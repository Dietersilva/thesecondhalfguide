(() => {
'use strict';
const TODAY={year:2026,month:9};
const MAIN=[62,65,67];
const YEARS=[62,63,64,65,66,67];
const MONTHS=['January','February','March','April','May','June','July','August','September','October','November','December'];
const $=id=>document.getElementById(id);
const MONEY_FMT=new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0});
const money=n=>MONEY_FMT.format(Number.isFinite(n)?n:0);
const pct=n=>`${Math.max(0,Math.min(999,Math.round(n)))}%`;
const num=(id,min=0,max=1e9)=>{const e=$(id),v=Number(e?.value);return Number.isFinite(v)?Math.max(min,Math.min(max,v)):min;};
const radio=name=>document.querySelector(`input[name="${name}"]:checked`)?.value||'';

const AWI={1980:12513.46,1990:21027.98,2000:32154.82,2010:41673.83,2015:48098.63,2020:55628.60,2021:60575.07,2022:63795.13,2023:66621.80,2024:69846.57,2025:72025.07,2026:75246.70,2027:78286.92,2028:81537.43,2029:85047.82,2030:88895.99,2031:92915.31,2032:96989.47,2033:101085.63,2034:105045.59,2035:109064.86};
const AWI_YEARS=Object.keys(AWI).map(Number).sort((a,b)=>a-b);
const INTERP_CACHE=new Map();
function interp(y){if(INTERP_CACHE.has(y))return INTERP_CACHE.get(y);if(AWI[y]){INTERP_CACHE.set(y,AWI[y]);return AWI[y];}let lo=AWI_YEARS[0],hi=AWI_YEARS[AWI_YEARS.length-1];for(let i=0;i<AWI_YEARS.length-1;i++){if(y>AWI_YEARS[i]&&y<AWI_YEARS[i+1]){lo=AWI_YEARS[i];hi=AWI_YEARS[i+1];break;}}let v;if(y<lo)v=AWI[lo]/Math.pow(1.045,lo-y);else if(y>hi)v=AWI[hi]*Math.pow(1.038,y-hi);else{const t=(y-lo)/(hi-lo);v=AWI[lo]*Math.pow(AWI[hi]/AWI[lo],t);}INTERP_CACHE.set(y,v);return v;}
function cbb(y){if(y<=1980)return 25900*Math.pow(1.05,y-1980);const known={2021:142800,2022:147000,2023:160200,2024:168600,2025:176100,2026:184500};if(known[y])return known[y];return 184500*(interp(y)/interp(2026));}
function fra(y){if(y>=1960)return 67;if(y<=1937)return 65;if(y<=1954)return 66;return 66+(y-1954)*2/12;}
function factor(age,y){const f=fra(y),m=Math.round((age-f)*12);if(m===0)return 1;if(m<0){const e=-m;return 1-Math.min(36,e)*(5/9/100)-Math.max(0,e-36)*(5/12/100);}return 1+Math.min(m,(70-f)*12)*(2/3/100);}
function populateMonths(){['birth-month','spouse-birth-month'].forEach(id=>{const e=$(id);if(!e||e.options.length)return;MONTHS.forEach((m,i)=>{const o=document.createElement('option');o.value=i+1;o.textContent=m;e.appendChild(o);});e.value='1';});}
function person(prefix=''){const spouse=prefix==='spouse-';return {birthMonth:num(prefix+'birth-month',1,12),birthYear:num(prefix+'birth-year',1930,1970),retireAge:num(prefix+'retire-age',62,75),wages:num(spouse?'spouse-wages':'wages'),futureWages:num(spouse?'spouse-future-wages':'future-wages'),years:num(prefix+'years-worked',10,45),override:num(spouse?'spouse-ss-override':'ss-override'),overrideAge:num(spouse?'spouse-ss-override-age':'ss-override-age',62,70),countable:num(spouse?'spouse-countable-earnings':'countable-earnings')};}
function quickPiaCalc(p){if(p.override>0)return p.override/Math.max(.1,factor(p.overrideAge,p.birthYear));const eligibility=p.birthYear+62,indexYear=eligibility-2,latest=Math.min(p.wages,cbb(TODAY.year)),start=TODAY.year-p.years+1,vals=[];for(let y=start;y<=TODAY.year;y++){let nominal=latest*(interp(y)/interp(TODAY.year))/Math.pow(1.02,TODAY.year-y);nominal=Math.min(nominal,cbb(y));vals.push(nominal*(y<indexYear?interp(indexYear)/interp(y):1));}for(let y=TODAY.year+1;y<=p.birthYear+p.retireAge;y++){const nominal=Math.min(p.futureWages||p.wages,cbb(y));vals.push(nominal*(y<indexYear?interp(indexYear)/interp(y):1));}vals.sort((a,b)=>b-a);const top=vals.slice(0,35);while(top.length<35)top.push(0);const aime=Math.floor(top.reduce((a,b)=>a+b,0)/420);const scale=interp(eligibility-2)/interp(2024),b1=Math.round(1286*scale),b2=Math.round(7749*scale);let pia=.9*Math.min(aime,b1);if(aime>b1)pia+=.32*Math.min(aime-b1,b2-b1);if(aime>b2)pia+=.15*(aime-b2);return Math.max(0,pia/Math.max(1,scale));}
const PIA_CACHE=new Map();
function quickPia(p){const k=[p.birthYear,p.retireAge,p.wages,p.futureWages,p.years,p.override,p.overrideAge].join('|');if(PIA_CACHE.has(k))return PIA_CACHE.get(k);const v=quickPiaCalc(p);PIA_CACHE.set(k,v);return v;}
function monthly(age,p){return quickPia(p)*factor(age,p.birthYear);}
function state(){return {household:radio('household'),earlyUse:radio('early-ss-use'),you:person(),spouse:person('spouse-'),traditional:num('traditional-balance'),roth:num('roth-balance'),taxable:num('taxable-balance'),contrib:num('annual-contrib'),employer:num('employer-contrib'),spouseContrib:num('spouse-contrib'),spouseEmployer:num('spouse-employer-contrib'),returnRate:num('return-rate',0,12)/100,pension:num('pension-income'),other:num('other-income'),spending:num('annual-spending'),horizon:num('horizon',80,100),limit:num('earnings-limit'),partB:$('part-b-start')?.value||'',hsa:!!$('hsa')?.checked,coverage:!!$('active-employer-coverage')?.checked,creditable:!!$('creditable-drug')?.checked,employerSize:$('employer-size')?.value||'unknown'};}
function ageNow(p){let a=TODAY.year-p.birthYear;if(TODAY.month<p.birthMonth)a--;return a;}
function grossAnnual(claimAge,p){return monthly(claimAge,p)*12;}
function withheldAnnual(claimAge,p,limit){if(claimAge>=fra(p.birthYear))return 0;return Math.min(grossAnnual(claimAge,p),Math.max(0,p.countable-limit)/2);}
function personAnnual(claimAge,p,applyTest,limit){return Math.max(0,grossAnnual(claimAge,p)-(applyTest?withheldAnnual(claimAge,p,limit):0));}
function ssAnnual(claimAge,s,applyTest=false){let total=personAnnual(claimAge,s.you,applyTest,s.limit);if(s.household==='couple')total+=personAnnual(claimAge,s.spouse,applyTest,s.limit);return total;}
function ssAnnualMixed(a,b,s,applyTest=false){let total=personAnnual(a,s.you,applyTest,s.limit);if(s.household==='couple')total+=personAnnual(b,s.spouse,applyTest,s.limit);return total;}
function contributions(s){return s.contrib+s.employer+(s.household==='couple'?s.spouseContrib+s.spouseEmployer:0);}
function baseAtRetirement(s,rate=s.returnRate){let bal=s.traditional+s.roth+s.taxable,years=Math.max(0,Math.round(s.you.retireAge-ageNow(s.you)));for(let i=0;i<years;i++){bal*=1+rate;bal+=contributions(s);}return bal;}
function portfolioForClaim(claimAge,s,rate=s.returnRate){let bal=s.traditional+s.roth+s.taxable;const start=ageNow(s.you),retire=s.you.retireAge;for(let age=start;age<retire;age++){bal*=1+rate;bal+=contributions(s);if(s.earlyUse==='invest'&&age>=claimAge)bal+=ssAnnual(claimAge,s,true);}const atRetire=bal;for(let age=Math.round(retire);age<s.horizon;age++){bal*=1+rate;const income=(age>=claimAge?ssAnnual(claimAge,s,false):0)+s.pension+s.other;bal-=Math.max(0,s.spending-income);if(bal<0){bal=0;break;}}return {atRetire,horizon:bal};}
function cumulative(claimAge,through,s){let t=0;for(let a=claimAge;a<=through;a++)t+=ssAnnual(claimAge,s,a<s.you.retireAge);return t;}
function cumulativeMixed(a,b,through,s){let t=0;const start=Math.min(a,b);for(let age=start;age<=through;age++){if(age>=a)t+=personAnnual(a,s.you,age<s.you.retireAge,s.limit);if(s.household==='couple'&&age>=b)t+=personAnnual(b,s.spouse,age<s.spouse.retireAge,s.limit);}return t;}
function cross(a,b,s){for(let age=b;age<=100;age++){if(cumulative(b,age,s)>=cumulative(a,age,s))return age;}return null;}
function incomeAtRetirement(claimAge,s){const ss=s.you.retireAge>=claimAge?ssAnnual(claimAge,s,false):0;return ss+s.pension+s.other;}
function spendingCoverage(claimAge,s){if(s.spending<=0)return 100;return incomeAtRetirement(claimAge,s)/s.spending*100;}

function ensureValuePanels(){
  if($('meaning-panel'))return;
  const results=document.querySelector('.rsm-results');
  const stats=results?.querySelector('.rsm-stat-grid');
  if(!results||!stats)return;
  const wrap=document.createElement('div');
  wrap.innerHTML=`
    <section class="rsm-panel" id="meaning-panel">
      <div class="rsm-section-head"><div><span class="rsm-kicker">Plain-English readout</span><h2>What this means for you</h2></div><p>Tradeoffs, not a one-size-fits-all answer.</p></div>
      <div id="meaning-results" class="rsm-notes"></div>
    </section>
    <section class="rsm-panel" id="goal-panel">
      <div class="rsm-section-head"><div><span class="rsm-kicker">Decision guide</span><h2>Which claiming age best fits your goal?</h2></div></div>
      <div id="goal-results" class="rsm-medicare-grid"></div>
    </section>
    <section class="rsm-panel" id="earnings-panel">
      <div class="rsm-section-head"><div><span class="rsm-kicker">Working while claiming</span><h2>Earnings-test impact</h2></div></div>
      <div id="earnings-results" class="rsm-notes"></div>
    </section>
    <section class="rsm-panel" id="stress-panel">
      <div class="rsm-section-head"><div><span class="rsm-kicker">Stress test</span><h2>What if investment returns are weaker or stronger?</h2></div><p>Illustration only, not a probability forecast.</p></div>
      <div class="rsm-table-wrap"><table class="rsm-table"><thead><tr><th>Return assumption</th><th>Claim 62</th><th>Claim 65</th><th>Claim 67</th></tr></thead><tbody id="stress-body"></tbody></table></div>
    </section>
    <section class="rsm-panel rsm-hidden" id="couple-panel">
      <div class="rsm-section-head"><div><span class="rsm-kicker">Couple planning</span><h2>What if you claim at different ages?</h2></div><p>Combined annual Social Security once both benefits have started.</p></div>
      <div class="rsm-table-wrap"><table class="rsm-table"><thead><tr><th>You / spouse</th><th>62</th><th>65</th><th>67</th></tr></thead><tbody id="couple-body"></tbody></table></div>
      <div id="survivor-watch" class="rsm-notes" style="margin-top:14px"></div>
    </section>
    <section class="rsm-panel" id="watch-panel">
      <div class="rsm-section-head"><div><span class="rsm-kicker">Taxes & Medicare</span><h2>Things the gross-income numbers do not show</h2></div></div>
      <div id="watch-results" class="rsm-medicare-grid"></div>
    </section>`;
  let ref=stats;
  [...wrap.children].forEach(el=>{ref.insertAdjacentElement('afterend',el);ref=el;});
}

function renderMeaning(s){
  const be65=cross(62,65,s),be67=cross(62,67,s),p62=portfolioForClaim(62,s),p67=portfolioForClaim(67,s);
  const earlyWithheld=withheldAnnual(62,s.you,s.limit)+(s.household==='couple'?withheldAnnual(62,s.spouse,s.limit):0);
  const lines=[];
  lines.push(`<div class="rsm-note"><strong>Cash sooner vs. larger check later:</strong> Claiming at 62 starts income earlier. Claiming at 67 produces about <b>${money(ssAnnual(67,s)-ssAnnual(62,s))} more per year</b> once both strategies are fully in pay status.</div>`);
  if(be65||be67)lines.push(`<div class="rsm-note"><strong>When waiting catches up:</strong> Under these inputs, 65 catches 62 at about <b>age ${be65||'100+'}</b>, while 67 catches 62 at about <b>age ${be67||'100+'}</b>.</div>`);
  lines.push(`<div class="rsm-note"><strong>Portfolio consequence:</strong> At age ${s.horizon}, the model shows about <b>${money(p62.horizon)}</b> remaining if you claim at 62 versus <b>${money(p67.horizon)}</b> if you claim at 67, using your spending and return assumptions.</div>`);
  if(earlyWithheld>0)lines.push(`<div class="rsm-note"><strong>Working matters:</strong> Your entered earnings could cause roughly <b>${money(earlyWithheld)}</b> of first-year benefits to be withheld if benefits begin at 62 under the current 2026 earnings-test limit.</div>`);
  $('meaning-results').innerHTML=lines.join('');
}
function renderGoals(s){const retire=s.you.retireAge,earningsImpact=withheldAnnual(62,s.you,s.limit)>0;$('goal-results').innerHTML=`<div class="rsm-medicare-card"><strong>Claim at 62</strong><p><b>Best fit for:</b> earlier cash flow or a shorter planning horizon.${earningsImpact?' Your entered work earnings make this option less attractive before FRA because benefits may be withheld.':''}</p></div><div class="rsm-medicare-card"><strong>Claim at 65</strong><p><b>Best fit for:</b> a middle path between earlier checks and a larger monthly benefit.${Math.abs(retire-65)<=1?' This also lines up closely with your entered retirement age.':''}</p></div><div class="rsm-medicare-card"><strong>Claim at 67</strong><p><b>Best fit for:</b> maximizing the monthly benefit among these three choices and placing more weight on longevity protection.</p></div>`;}
function renderEarnings(s){const people=[['You',s.you]];if(s.household==='couple')people.push(['Spouse',s.spouse]);$('earnings-results').innerHTML=people.map(([label,p])=>{const w62=withheldAnnual(62,p,s.limit),gross=grossAnnual(62,p),net=Math.max(0,gross-w62);if(w62<=0)return `<div class="rsm-note"><strong>${label}:</strong> Entered countable earnings are not above the current ${money(s.limit)} annual limit, so this simple 62 illustration shows no earnings-test withholding.</div>`;return `<div class="rsm-note"><strong>${label} at 62:</strong> Estimated gross benefit ${money(gross)}/yr. With entered countable earnings, roughly <b>${money(w62)}</b> could be withheld, leaving about <b>${money(net)}</b> paid before tax in this simplified full-year illustration. Benefits withheld under the earnings test are not simply lost; SSA later adjusts for months withheld at full retirement age.</div>`;}).join('');}
function renderStress(s){const base=s.returnRate,low=Math.max(0,base-.02),high=Math.min(.10,base+.02);const rows=[['Conservative',low],['Your assumption',base],['Stronger return',high]];$('stress-body').innerHTML=rows.map(([label,rate])=>`<tr><td><strong>${label}</strong><br><small>${(rate*100).toFixed(1)}% annual return</small></td>${MAIN.map(age=>{const v=portfolioForClaim(age,s,rate).horizon;return `<td>${v>0?money(v):'<b>$0</b><br><small>portfolio depleted before horizon</small>'}</td>`;}).join('')}</tr>`).join('');}
function renderCouple(s){const panel=$('couple-panel');if(s.household!=='couple'){panel?.classList.add('rsm-hidden');return;}panel?.classList.remove('rsm-hidden');$('couple-body').innerHTML=MAIN.map(a=>`<tr><td><strong>You ${a}</strong></td>${MAIN.map(b=>`<td>${money(ssAnnualMixed(a,b,s,false))}/yr<br><small>${money(cumulativeMixed(a,b,s.horizon,s))} cumulative by ${s.horizon}</small></td>`).join('')}</tr>`).join('');$('survivor-watch').innerHTML=`<div class="rsm-note"><strong>Survivor-income watch:</strong> A two-benefit household can become a one-benefit household after the first death. The exact survivor amount depends on claiming ages and survivor rules, so this matrix intentionally does not pretend the two checks simply continue. Treat survivor planning as a separate decision before choosing a couple strategy.</div>`;}
function renderWatch(s){const roughEarnings=s.you.wages+(s.household==='couple'?s.spouse.wages:0),irmaa=s.household==='couple'?218000:109000;const items=[];items.push(`<div class="rsm-medicare-card"><strong>Social Security can be taxable</strong><p>The benefits shown above are gross. Depending on other income, part of Social Security can be included in federal taxable income. The model does not subtract federal or state tax from the comparison yet.</p></div>`);items.push(`<div class="rsm-medicare-card${roughEarnings>irmaa?' rsm-alert':''}"><strong>IRMAA review</strong><p>For 2026, higher Medicare premiums begin above ${money(irmaa)} of MAGI for ${s.household==='couple'?'married joint':'individual'} filers. Your entered Social Security-covered earnings total ${money(roughEarnings)}. Earnings are <b>not</b> the same as MAGI, so this is a review flag, not an IRMAA calculation. Medicare generally uses tax information from two years earlier.</p></div>`);items.push(`<div class="rsm-medicare-card"><strong>Current Part B benchmark</strong><p>The standard Medicare Part B premium is $202.90 per month in 2026. Future premiums at your actual enrollment date will be different, so the calculator does not bake today's premium into a long-range retirement forecast.</p></div>`);$('watch-results').innerHTML=items.join('');}
function renderTable(s){$('strategy-body').innerHTML=MAIN.map(age=>{const p=portfolioForClaim(age,s),be=age===62?'Baseline':(cross(62,age,s)?`About age ${cross(62,age,s)}`:'After 100');return `<tr${age===65?' class="rsm-focus-row"':''}><td>${age}${age===65?'<br><span class="rsm-tag">middle option</span>':''}</td><td>${money(ssAnnual(age,s)/12)}/mo</td><td>${money(ssAnnual(age,s))}</td><td>${be}</td><td>${money(p.atRetire)}</td><td>${money(p.horizon)}</td></tr>`;}).join('');}
function renderIncome(s){$('income-results').innerHTML=MAIN.map(age=>{const guaranteed=incomeAtRetirement(age,s),gap=Math.max(0,s.spending-guaranteed),coverage=spendingCoverage(age,s);const bridge=s.you.retireAge<age?`<br><small>Social Security has not started at retirement; bridge ${Math.max(0,age-s.you.retireAge).toFixed(0)} year(s) first.</small>`:'';return `<div class="rsm-medicare-card"><strong>Claim at ${age}</strong><p>Income available at retirement: <b>${money(guaranteed)}</b><br>Spending target: <b>${money(s.spending)}</b><br>Covered without portfolio withdrawals: <b>${pct(coverage)}</b><br>${gap?`Initial portfolio need: <b>${money(gap)}/yr</b>`:'Entered guaranteed income covers the spending target.'}${bridge}</p></div>`;}).join('');}
function renderRoadmap(s){$('roadmap').innerHTML=YEARS.map(age=>`<div class="rsm-roadmap-item"><div class="rsm-age">Age ${age}</div><div class="rsm-rail"><span class="rsm-dot"></span></div><div class="rsm-roadmap-copy">Estimated Social Security if started at ${age}: <strong>${money(ssAnnual(age,s))}/yr</strong>${age===64?' Begin detailed Medicare and HSA timing review.':''}${age===65?' Medicare eligibility is a separate decision from Social Security claiming.':''}</div></div>`).join('');}
function renderChart(s){const svg=$('crossover-chart'),W=820,H=360,p={l:70,r:20,t:20,b:42},end=s.horizon,ages=[];for(let a=62;a<=end;a++)ages.push(a);const series=MAIN.map(a=>({a,vals:ages.map(x=>x<a?0:cumulative(a,x,s))})),max=Math.max(1,...series.flatMap(z=>z.vals)),x=a=>p.l+(a-62)/(end-62)*(W-p.l-p.r),y=v=>H-p.b-v/max*(H-p.t-p.b);let out='';for(let i=0;i<=4;i++){const yy=p.t+(H-p.t-p.b)*i/4;out+=`<line x1="${p.l}" y1="${yy}" x2="${W-p.r}" y2="${yy}" stroke="var(--line)"/>`;}const colors=['var(--pine)','var(--gold)','var(--ink-soft)'],dash=['','9 5','3 4'];series.forEach((z,i)=>{let d='';ages.forEach((a,j)=>d+=`${j?'L':'M'}${x(a)} ${y(z.vals[j])} `);out+=`<path d="${d}" fill="none" stroke="${colors[i]}" stroke-width="3" stroke-dasharray="${dash[i]}"/>`;});[62,65,67,70,80,end].filter((v,i,a)=>v<=end&&a.indexOf(v)===i).forEach(a=>out+=`<text x="${x(a)}" y="${H-14}" text-anchor="middle" font-size="12" fill="var(--ink-faint)">${a}</text>`);svg.innerHTML=out;}
function renderMedicare(s){const y=s.you.birthYear+65,m=s.you.birthMonth,c=[];c.push(`<div class="rsm-medicare-card"><strong>Age 65</strong><p>Your Medicare eligibility month is approximately <b>${MONTHS[m-1]} ${y}</b>. Review enrollment timing about 3 months before.</p></div>`);if(s.coverage)c.push(`<div class="rsm-medicare-card"><strong>Active employer coverage</strong><p>Whether Part B can be delayed safely depends on the active employer plan and employer size. Confirm with the plan administrator.</p></div>`);if(s.hsa)c.push(`<div class="rsm-medicare-card rsm-alert"><strong>HSA warning</strong><p>Coordinate HSA contributions before Medicare enrollment because retroactive Part A can matter.</p></div>`);if(!s.creditable)c.push(`<div class="rsm-medicare-card rsm-alert"><strong>Part D</strong><p>Confirm whether existing drug coverage is creditable before delaying Part D.</p></div>`);$('medicare-results').innerHTML=c.join('');}
function renderNotes(s){$('strategy-notes').innerHTML=`<div class="rsm-note"><strong>Social Security:</strong> ${s.you.override>0?'Using the SSA estimate you entered.':'Using an SSA-style estimate from covered earnings, work history, wage indexing and a 35-year benefit calculation.'}</div><div class="rsm-note"><strong>Portfolio:</strong> The model starts with ${money(s.traditional+s.roth+s.taxable)}, adds entered contributions while working, then fills any retirement-spending gap after Social Security, pension and other income.</div>${s.household==='couple'?'<div class="rsm-note"><strong>Couples:</strong> The main 62/65/67 table keeps the comparison simple by using the same claim age for both people. The couple matrix above adds mixed claim ages, but detailed survivor-benefit optimization remains separate.</div>':''}<div class="rsm-note"><strong>Official check:</strong> Before filing, compare these estimates with your personal Social Security statement and current SSA/Medicare rules.</div>`;}
function render(){const s=state();$('spouse-inputs')?.classList.toggle('rsm-hidden',s.household!=='couple');document.querySelectorAll('.spouse-advanced').forEach(e=>e.classList.toggle('rsm-hidden',s.household!=='couple'));$('focus-age').textContent=Math.round(s.you.retireAge);$('current-balance').textContent=money(s.traditional+s.roth+s.taxable);$('projected-balance').textContent=money(baseAtRetirement(s));$('ss-at-65').textContent=money(ssAnnual(65,s))+'/yr';$('spending-target').textContent=money(s.spending)+'/yr';$('break-even-65').textContent=`about age ${cross(62,65,s)||'100+'}`;$('break-even-67').textContent=`about age ${cross(62,67,s)||'100+'}`;$('break-even-65-67').textContent=`about age ${cross(65,67,s)||'100+'}`;$('horizon-display').textContent='Age '+s.horizon;$('summary-copy').textContent=`Using ${s.you.override>0?'your SSA estimate':'an SSA-style estimate'}, compare claiming at 62, 65 and 67 while your savings, contributions, retirement income and spending assumptions flow through the model.`;renderMeaning(s);renderGoals(s);renderEarnings(s);renderStress(s);renderCouple(s);renderWatch(s);renderTable(s);renderIncome(s);renderRoadmap(s);renderChart(s);renderMedicare(s);renderNotes(s);}
function previewLinks(){const p=new URLSearchParams(location.search),token=p.get('_vercel_share');if(!token)return;document.querySelectorAll('a[href^="/"]').forEach(a=>{const u=new URL(a.getAttribute('href'),location.origin);u.searchParams.set('_vercel_share',token);a.href=u.pathname+u.search+u.hash;});}
let renderTimer=0;function scheduleRender(){clearTimeout(renderTimer);renderTimer=setTimeout(()=>requestAnimationFrame(render),180);}function immediateRender(){clearTimeout(renderTimer);requestAnimationFrame(render);}function init(){populateMonths();ensureValuePanels();previewLinks();document.querySelectorAll('.rsm-page input,.rsm-page select').forEach(e=>{const live=e.matches('input[type=number],input[type=text],input[type=month]');if(live)e.addEventListener('input',scheduleRender);e.addEventListener('change',immediateRender);});$('print-report')?.addEventListener('click',()=>window.print());$('rsm-reset')?.addEventListener('click',()=>location.reload());render();}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();